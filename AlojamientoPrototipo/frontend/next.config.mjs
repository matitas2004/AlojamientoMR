/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ya no necesitamos rewrites porque usamos API Route Handlers
  // en src/app/api/ que hacen de proxy server-side
};

export default nextConfig;
